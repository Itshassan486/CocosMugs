<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Coco’s Mugs – Put your creativity on a mug</title>
  <meta name="description" content="Upload your photo and get it on a mug—original or transformed into Stunning Art. Coco’s Mugs." />
  <style>
    :root{
      --cream:#F7EEDD;
      --tan:#EAD9B9;          /* light beige */
      --tan-dark:#D9C4A0;     /* darker beige for brand bar */
      --brown:#5B3A1E;
      --brown-2:#7A4B26;
      --ink:#2a1a0f;
      --white:#ffffff;
      --border:#ecdcc4;
    }

    *{ box-sizing:border-box }
    html,body{
      margin:0; font-family:system-ui,-apple-system,Segoe UI,Roboto,Inter,Arial,sans-serif;
      color:var(--ink); background:var(--white); scroll-behavior:smooth;
    }

    /* Brand bar */
    header{
      position:sticky; top:0; z-index:10;
      background:var(--tan-dark);
      border-bottom:1px solid var(--border);
    }
    .wrap{ max-width:1200px; margin:auto; padding:16px 20px }
    .brand{ display:flex; flex-direction:column; align-items:center; gap:8px }
    .brand h1{
      margin:0; font-size:46px; line-height:1; color:var(--brown);
      letter-spacing:.3px; text-align:center;
    }

    /* Hero */
    .hero{ padding:72px 20px; text-align:center }
    .tag{
      display:inline-block; background:var(--tan); color:var(--ink);
      padding:6px 12px; border-radius:999px; font-size:14px; margin-bottom:14px
    }
    h2.headline{ font-size:46px; line-height:1.1; margin:8px 0 14px; color:var(--brown) }
    .desc{ font-size:18px; max-width:70ch; margin:0 auto; color:#63492f }
    .cta{ display:flex; justify-content:center; gap:16px; flex-wrap:wrap; margin-top:26px }
    .btn{
      appearance:none; border:0; border-radius:14px; padding:18px 28px;
      font-weight:800; cursor:pointer; font-size:18px;
      transition: transform .04s ease, box-shadow .15s ease, background .15s ease;
    }
    .btn:active{ transform: translateY(1px) }
    .btn.primary{ background:var(--brown); color:var(--cream) }
    .btn.primary:hover{ background:var(--brown-2); box-shadow:0 0 0 3px var(--tan) }

    /* Sections */
    .section{ padding:56px 20px }
    .section h3{ font-size:28px; color:var(--brown); margin:0 0 18px; text-align:center }

    /* Gallery */
    .grid{ display:grid; gap:22px }
    .grid-3{ grid-template-columns:repeat(3,minmax(0,1fr)) } /* 3-column layout */
    .card{
      background:var(--white); border:1px solid var(--border); border-radius:18px;
      overflow:hidden; box-shadow:0 10px 24px rgba(91,58,30,.08);
    }
    .pair{ display:grid; grid-template-columns:1fr 1fr }
    figure{ position:relative; margin:0 }
    figure img{ display:block; width:100%; height:260px; object-fit:cover }
    .label{
      position:absolute; top:10px; left:10px;
      background:var(--brown); color:var(--cream);
      font-size:12px; padding:6px 10px; border-radius:999px
    }

    /* Footer */
    footer{ padding:22px 20px; border-top:1px solid var(--border); color:#654922; text-align:center }

    /* Animations */
    .fade-up{ opacity:0; transform:translateY(40px); animation:fadeUp 0.9s ease-out forwards }
    @keyframes fadeUp{ to{ opacity:1; transform:translateY(0) } }
    .d1{ animation-delay:.15s } .d2{ animation-delay:.3s } .d3{ animation-delay:.45s } .d4{ animation-delay:.6s }

    @media (max-width:1100px){
      .grid-3{ grid-template-columns:1fr 1fr } /* fallback for tablets */
    }
    @media (max-width:700px){
      .grid-3{ grid-template-columns:1fr } /* fallback for phones */
      figure img{ height:220px }
    }
  </style>
</head>
<body>
  <!-- Brand name only -->
  <header>
    <div class="wrap">
      <div class="brand fade-up d1">
        <h1>Coco’s Mugs</h1>
      </div>
    </div>
  </header>

  <!-- Hero -->
  <section class="hero">
    <div class="wrap">
      <span class="tag fade-up d1">Handmade • Printed with love</span>
      <h2 class="headline fade-up d2">Put your creativity on a mug.</h2>
      <p class="desc fade-up d3">
        Upload a photo of your pet, person, or favorite moment. Keep it <strong>original</strong> or transform it into <strong>Stunning Art</strong>—then we print it on a premium ceramic mug.
      </p>
      <div class="cta fade-up d4">
        <a class="btn primary" href="order.html">Start right now</a>
      </div>
    </div>
  </section>

  <!-- Gallery -->
  <section id="gallery" class="section">
    <div class="wrap">
      <h3 class="fade-up d1">Real-world before & afters</h3>
      <div class="grid grid-3">
        <!-- Pair 1: Kitten -->
        <div class="card pair fade-up d2">
          <figure>
            <span class="label">Before</span>
            <img src="assets/kitten-before.jpg" alt="Before — kitten">
          </figure>
          <figure>
            <span class="label">After</span>
            <img src="assets/kitten-after.jpg" alt="After — kitten mug">
          </figure>
        </div>

        <!-- Pair 2: Couple -->
        <div class="card pair fade-up d3">
          <figure>
            <span class="label">Before</span>
            <img src="assets/couple-before.jpg" alt="Before — couple photo">
          </figure>
          <figure>
            <span class="label">After</span>
            <img src="assets/couple-after.jpg" alt="After — couple mug">
          </figure>
        </div>

        <!-- Pair 3: Family -->
        <div class="card pair fade-up d4">
          <figure>
            <span class="label">Before</span>
            <img src="assets/family-before.jpg" alt="Before — family photo">
          </figure>
          <figure>
            <span class="label">After</span>
            <img src="assets/family-after.jpg" alt="After — family mug">
          </figure>
        </div>
      </div>
    </div>
  </section>

  <footer>
    <div class="wrap">
      <small>© <span id="year"></span> Coco’s Mugs. All rights reserved.</small>
    </div>
  </footer>

  <script>
    document.getElementById('year').textContent = new Date().getFullYear();
  </script>
</body>
</html>
